describe Drawdown do
  let(:peak)     { }
  let(:trough)   { }
  let(:drawdown) { }

  it "calculates percentage correctly" do
    expect(drawdown.to_f).to eq
  end

  it "formats string" do
    expect(drawdown.to_s).to eq
  end
end